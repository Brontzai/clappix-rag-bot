services:
  bot:
    build: .
    env_file:
      - .env
    volumes:
      - chroma_data:/app/chroma_data
      - ./data:/app/data
    ports:
      - "8080:8080"
    restart: unless-stopped

volumes:
  chroma_data:
